class CRUD {
    constructor(url = "https://tribe.api.fdnd.nl/v1/") {
        this.url = url;
    }

    /**
     * Method to create a entry through HTTP POST method
     *
     *
     */
    Create() {}

    /**
     * Method to get an entries through HTTP GET method
     *
     *
     */
    Read() {}

    /**
     * Method to update an entry through HTTP PATCH method
     *
     *
     */
    Update() {}

    /**
     * Method to delete an entry through HTTP DELETE method
     *
     *
     */
    Delete() {}
}
